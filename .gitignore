# Logs
logs
*.log
npm-debug.log*

node_modules
dist-ssr
dist-server

# dist/ SÍ se versiona: el despliegue toma el build del repo y Express lo sirve
# desde dist/ (ver server.ts). Regenerar con `npm run build` ANTES de commitear,
# o se publica una interfaz vieja aunque el fuente esté al día.

# Secrets — NUNCA versionar
.env
.env.local

# Bases de datos SQLite locales (nunca versionar — contienen datos/PII)
*.db
*.db-shm
*.db-wal

# Artefactos temporales de scripts / OneDrive
# Solo en la raíz: sin la barra inicial esto se tragaría cualquier _algo.json
# dentro de src/, sin avisar.
/_*.json
.849*
.codex-inspect/

# Uploads de usuarios
uploads/

# Claude Code: permisos por máquina. .claude/settings.json SÍ se versiona (es la config
# compartida del proyecto); el .local es de cada equipo y sus rutas no sirven en otro.
.claude/settings.local.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
